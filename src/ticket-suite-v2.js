const fs=require('fs'),path=require('path');const {ChannelType,PermissionFlagsBits,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,StringSelectMenuBuilder}=require('discord.js');
module.exports=function attachTicketSuite(client,prefix='.'){
 const P=prefix||'.';const file=path.join(process.cwd(),'data','ticket-suite-v2.json');fs.mkdirSync(path.dirname(file),{recursive:true});let db={};try{db=JSON.parse(fs.readFileSync(file,'utf8')||'{}')}catch{};const save=()=>fs.writeFileSync(file,JSON.stringify(db,null,2));const fresh=()=>({categories:[{id:'support',name:'Support',emoji:'🎫',description:'General support',category:null,role:null}],panel:{title:'🎫 Support Center',description:'Select a ticket category below to open a private ticket.',color:0x5865f2,buttonLabel:'Open Ticket',buttonEmoji:'🎫'},applications:{enabled:false,channel:null,title:'📝 Staff Application',description:'Apply for staff by answering the questions below.',color:0x5865f2,questions:['What are you applying for?','Why should we choose you?','What experience do you have?']},counter:0});const gd=id=>{const d=db[id]||(db[id]=fresh()),f=fresh();for(const k of Object.keys(f))if(d[k]===undefined)d[k]=f[k];return d};
 const can=m=>m.member.permissions.has(PermissionFlagsBits.ManageGuild)||m.guild.ownerId===m.author.id;
 const panel=d=>({embeds:[new EmbedBuilder().setTitle(d.panel.title).setDescription(d.panel.description).setColor(d.panel.color).setFooter({text:'LightCore Ticket System'})],components:[new ActionRowBuilder().addComponents(new StringSelectMenuBuilder().setCustomId('lc_ticket_category').setPlaceholder('🎫 Select a ticket category').addOptions(d.categories.slice(0,25).map(x=>({label:x.name.slice(0,100),value:x.id,description:x.description.slice(0,100),emoji:x.emoji||'🎫'}))))]});
 async function setup(m){if(!can(m))return m.reply('❌ You need Manage Server.');const d=gd(m.guild.id);for(const c of d.categories){let cat=c.category&&m.guild.channels.cache.get(c.category);if(!cat)cat=await m.guild.channels.create({name:`🎫・${c.name}`.slice(0,100),type:ChannelType.GuildCategory});c.category=cat.id;let role=c.role&&m.guild.roles.cache.get(c.role);if(!role)role=m.guild.roles.cache.find(r=>r.name.toLowerCase()===`${c.name.toLowerCase()} staff`);if(!role)role=await m.guild.roles.create({name:`${c.name} Staff`.slice(0,100),reason:'LightCore ticket category'});c.role=role.id}save();return m.reply('✅ Ticket v2 categories are configured. Use `.ticket panel` to publish the editable panel.')}
 async function open(m,id){const d=gd(m.guild.id),c=d.categories.find(x=>x.id===id)||d.categories[0];if(!c.category)return m.reply(`❌ Run ${P}ticket setup first.`);const existing=m.guild.channels.cache.find(x=>x.type===ChannelType.GuildText&&x.parentId===c.category&&x.topic===`ticket:${m.author.id}`);if(existing)return m.reply(`⚠️ You already have a ticket in this category: ${existing}`);d.counter++;const ch=await m.guild.channels.create({name:`${c.id}-${String(d.counter).padStart(4,'0')}`.slice(0,100),type:ChannelType.GuildText,parent:c.category,topic:`ticket:${m.author.id}`,permissionOverwrites:[{id:m.guild.roles.everyone.id,deny:[PermissionFlagsBits.ViewChannel]},{id:m.author.id,allow:[PermissionFlagsBits.ViewChannel,PermissionFlagsBits.SendMessages,PermissionFlagsBits.ReadMessageHistory,PermissionFlagsBits.AttachFiles]},{id:c.role,allow:[PermissionFlagsBits.ViewChannel,PermissionFlagsBits.SendMessages,PermissionFlagsBits.ReadMessageHistory,PermissionFlagsBits.ManageMessages]}]});const row=new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('lc_ticket_close').setLabel('Close').setEmoji('🔒').setStyle(ButtonStyle.Danger),new ButtonBuilder().setCustomId('lc_ticket_claim').setLabel('Claim').setEmoji('🙋').setStyle(ButtonStyle.Primary),new ButtonBuilder().setCustomId('lc_ticket_add').setLabel('Add Member').setEmoji('➕').setStyle(ButtonStyle.Secondary));await ch.send({content:`Welcome <@${m.author.id}> • **${c.name}**`,embeds:[new EmbedBuilder().setTitle(`🎫 ${c.name} Ticket`).setDescription('Please describe your issue clearly. Staff will assist you.')],components:[row]});save();return m.reply(`✅ Ticket created: ${ch}`)}
 async function manage(m,c,a){const d=gd(m.guild.id);if(c==='setup')return setup(m);if(c==='panel')return m.channel.send(panel(d));if(c==='open'||c==='create')return open(m,a[0]||d.categories[0].id);if(c==='category'||c==='categories')return m.reply({embeds:[new EmbedBuilder().setTitle('🎫 Ticket Categories').setDescription(d.categories.map(x=>`${x.emoji} **${x.name}** — \`${x.id}\`\n${x.description}`).join('\n\n'))]});if(c==='addcategory'){if(!can(m))return m.reply('❌ Manage Server required.');const id=(a.shift()||'').toLowerCase().replace(/[^a-z0-9-]/g,'').slice(0,30),name=(a.shift()||'Support').slice(0,80),description=a.join(' ')||'Ticket category';if(!id)return m.reply(`Usage: ${P}ticket addcategory <id> <name> <description>`);if(d.categories.some(x=>x.id===id))return m.reply('❌ That category already exists.');d.categories.push({id,name,emoji:'🎫',description,category:null,role:null});save();return setup(m)}if(c==='delcategory'){if(!can(m))return m.reply('❌ Manage Server required.');const id=a[0];if(d.categories.length<=1)return m.reply('❌ Keep at least one category.');d.categories=d.categories.filter(x=>x.id!==id);save();return m.reply('✅ Ticket category deleted.')}if(c==='editpanel'){if(!can(m))return m.reply('❌ Manage Server required.');const [title,...rest]=a;const desc=rest.join(' ')||d.panel.description;if(title)d.panel.title=title.replace(/_/g,' ');d.panel.description=desc.replace(/_/g,' ');save();return m.reply('✅ Ticket panel updated. Use `.ticket panel` to publish it.')}if(c==='close'){await m.channel.permissionOverwrites.edit(m.guild.roles.everyone,{ViewChannel:false});return m.reply('🔒 Ticket closed.')}if(c==='claim'){return m.reply(`🙋 Ticket claimed by ${m.author}.`)}if(c==='unclaim')return m.reply('↩️ Ticket unclaimed.');if(c==='rename'){const n=a.join('-').toLowerCase().replace(/[^a-z0-9-]/g,'').slice(0,90);if(!n)return m.reply(`Usage: ${P}ticket rename <name>`);await m.channel.setName(n);return m.reply('✏️ Ticket renamed.')}if(c==='add'){const u=m.mentions.members.first();if(!u)return m.reply(`Usage: ${P}ticket add @user`);await m.channel.permissionOverwrites.edit(u.id,{ViewChannel:true,SendMessages:true,ReadMessageHistory:true});return m.reply(`➕ Added ${u}.`)}if(c==='remove'){const u=m.mentions.members.first();if(!u)return m.reply(`Usage: ${P}ticket remove @user`);await m.channel.permissionOverwrites.delete(u.id).catch(()=>{});return m.reply(`➖ Removed ${u}.`)}return m.reply(`🎫 **Ticket v2**\n${P}ticket setup\n${P}ticket panel\n${P}ticket categories\n${P}ticket addcategory <id> <name> <description>\n${P}ticket delcategory <id>\n${P}ticket editpanel <title> <description>\n${P}ticket open <category-id>\n${P}ticket claim / unclaim / close\n${P}ticket add @user / remove @user\n${P}ticket rename <name>`)}
 async function app(m,a){const d=gd(m.guild.id),sub=(a.shift()||'setup').toLowerCase();if(sub==='setup'){if(!can(m))return m.reply('❌ Manage Server required.');d.applications.enabled=true;d.applications.channel=m.channel.id;save();return m.reply(`✅ Application system enabled in ${m.channel}.`)}if(sub==='panel'){if(!can(m))return m.reply('❌ Manage Server required.');return m.channel.send({embeds:[new EmbedBuilder().setTitle(d.applications.title).setDescription(d.applications.description).setColor(d.applications.color)],components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('lc_apply_open').setLabel('Apply Now').setEmoji('📝').setStyle(ButtonStyle.Primary))]})}if(sub==='edit'){if(!can(m))return m.reply('❌ Manage Server required.');const [title,...rest]=a;d.applications.title=(title||d.applications.title).replace(/_/g,' ');if(rest.length)d.applications.description=rest.join(' ').replace(/_/g,' ');save();return m.reply('✅ Application panel updated. Use `.application panel` to publish it.')}if(sub==='questions')return m.reply({embeds:[new EmbedBuilder().setTitle('📝 Application Questions').setDescription(d.applications.questions.map((q,i)=>`**${i+1}.** ${q}`).join('\n'))]});if(!d.applications.enabled)return m.reply(`❌ Run ${P}application setup first.`);return openApplication(m,d)}
 async function openApplication(m,d){const ch=await m.guild.channels.create({name:`application-${m.author.username}`.toLowerCase().replace(/[^a-z0-9-]/g,'').slice(0,80),type:ChannelType.GuildText,parent:d.categories[0].category||null,permissionOverwrites:[{id:m.guild.roles.everyone.id,deny:[PermissionFlagsBits.ViewChannel]},{id:m.author.id,allow:[PermissionFlagsBits.ViewChannel,PermissionFlagsBits.SendMessages,PermissionFlagsBits.ReadMessageHistory]}]});await ch.send({embeds:[new EmbedBuilder().setTitle(d.applications.title).setDescription(`${d.applications.description}\n\n${d.applications.questions.map((q,i)=>`**${i+1}. ${q}**`).join('\n')}`)]});return m.reply(`✅ Application channel: ${ch}`)}
 const old=client.listeners('messageCreate').at(-1);if(old){client.removeListener('messageCreate',old);client.on('messageCreate',async m=>{try{if(m.author.bot||!m.guild)return;if(!m.content.startsWith(P))return old(m);const a=m.content.slice(P.length).trim().split(/\s+/),c=(a.shift()||'').toLowerCase();if(c==='ticket'||c==='tickets')return manage(m,(a.shift()||'help').toLowerCase(),a);if(c==='application'||c==='applications')return app(m,a);if(c==='apply')return app(m,['apply']);return old(m)}catch(e){console.error('[TICKET V2]',e)}})}
 client.on('interactionCreate',async i=>{try{if(i.isStringSelectMenu()&&i.customId==='lc_ticket_category'){await i.deferReply({ephemeral:true});return open({guild:i.guild,author:i.user,member:i.member,channel:i.channel,reply:x=>i.editReply(x)},i.values[0])}if(i.isButton()&&i.customId==='lc_apply_open'){return i.reply({content:`Use ${P}apply to start your application.`,ephemeral:true})}if(i.isButton()&&['lc_ticket_close','lc_ticket_claim','lc_ticket_add'].includes(i.customId)){if(i.customId==='lc_ticket_close'){await i.channel.permissionOverwrites.edit(i.guild.roles.everyone,{ViewChannel:false});return i.reply({content:'🔒 Ticket closed.',ephemeral:true})}return i.reply({content:`Use ${P}ticket ${i.customId==='lc_ticket_claim'?'claim':'add @user'} for this action.`,ephemeral:true})}}catch(e){console.error('[TICKET INTERACTION]',e)}});
};
