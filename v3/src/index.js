require('dotenv').config();
const http=require('http'),fs=require('fs'),path=require('path');
const {Client,GatewayIntentBits,EmbedBuilder,PermissionFlagsBits,ActionRowBuilder,ButtonBuilder,ButtonStyle,REST,Routes}=require('discord.js');
const Database=require('better-sqlite3');
const {slashCommands,prefixCommands,aliasMap,helpPayload,commandListEmbed,serversEmbed,adminNames}=require('./commands');
const statistics=require('./modules/statistics'),invites=require('./modules/invites'),features=require('./modules/features'),leveling=require('./modules/leveling'),logging=require('./modules/logging'),tickets=require('./modules/tickets'),applications=require('./modules/applications');
const token=process.env.DISCORD_TOKEN;if(!token)throw new Error('DISCORD_TOKEN missing');
const prefix=process.env.PREFIX||'.',startedAt=Date.now(),dbFile=process.env.DB_FILE||'./data/bot.sqlite';
fs.mkdirSync(path.dirname(path.resolve(dbFile)),{recursive:true});const db=new Database(dbFile);db.pragma('journal_mode = WAL');
db.exec(`CREATE TABLE IF NOT EXISTS warns(guild TEXT,user TEXT,reason TEXT,by TEXT,at INTEGER);CREATE TABLE IF NOT EXISTS money(user TEXT PRIMARY KEY,balance INTEGER DEFAULT 0,daily INTEGER DEFAULT 0);CREATE TABLE IF NOT EXISTS xp(guild TEXT,user TEXT,xp INTEGER DEFAULT 0,level INTEGER DEFAULT 0,PRIMARY KEY(guild,user));CREATE TABLE IF NOT EXISTS afk(user TEXT PRIMARY KEY,message TEXT,at INTEGER);CREATE TABLE IF NOT EXISTS giveaways(message TEXT PRIMARY KEY,guild TEXT,channel TEXT,host TEXT,prize TEXT,winners INTEGER DEFAULT 1,end_at INTEGER);`);
const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMembers,GatewayIntentBits.GuildMessages,GatewayIntentBits.MessageContent,GatewayIntentBits.GuildVoiceStates,GatewayIntentBits.GuildPresences]});
statistics.ensure(db);invites.ensure(db);features.ensureSchema(db);tickets.init(db);applications.init(db);logging.init(db);
const E=(t,d)=>new EmbedBuilder().setTitle(t).setDescription(d).setColor(0x5865f2).setTimestamp();
const inviteUrl=g=>`https://discord.com/oauth2/authorize?client_id=${process.env.CLIENT_ID||client.user.id}&scope=bot%20applications.commands&permissions=8${g?`&guild_id=${g}&disable_guild_select=true`:''}`;
const allowed=(i,n)=>!i.guild?['help','ping','invite','uptime','botinfo','servers'].includes(n):!adminNames.has(n)||!!(i.member?.permissions?.has(PermissionFlagsBits.Administrator)||i.member?.permissions?.has(PermissionFlagsBits.ManageGuild)||i.member?.permissions?.has(PermissionFlagsBits.ManageMessages));
function wallet(id){let r=db.prepare('SELECT * FROM money WHERE user=?').get(id);if(!r){db.prepare('INSERT INTO money(user,balance,daily) VALUES(?,?,?)').run(id,0,0);r=db.prepare('SELECT * FROM money WHERE user=?').get(id);}return r;}
async function run(i,raw){const n=aliasMap.get(raw)||raw;if(!allowed(i,n))return i.reply({content:'⛔ You do not have permission to use this command.',ephemeral:true});
if(n==='help')return i.reply(helpPayload());if(n==='ping')return i.reply(`🏓 **${client.ws.ping}ms**`);if(n==='uptime'){const s=Math.floor((Date.now()-startedAt)/1000);return i.reply({embeds:[E('⏱️ Uptime',`**${Math.floor(s/86400)}d ${Math.floor(s%86400/3600)}h ${Math.floor(s%3600/60)}m ${s%60}s**`)]});}
if(n==='invite')return i.reply({embeds:[E('🤖 Aarav All-In-One','Invite the bot using the button below.')],components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setLabel('Invite Bot').setStyle(ButtonStyle.Link).setURL(inviteUrl()))]});
if(n==='servers')return i.reply(serversEmbed(client));if(n==='botinfo')return i.reply({embeds:[E('🤖 Aarav All-In-One',`Servers: **${client.guilds.cache.size}**\nSlash: **${slashCommands.length}**\nClient: **${prefixCommands.length}**\nPrefix: \`${prefix}\``)]});
if(n==='membercount')return i.reply(`👥 **${i.guild.memberCount}** members`);if(n==='serverinfo')return i.reply({embeds:[E(`🌐 ${i.guild.name}`,`Members: **${i.guild.memberCount}**\nChannels: **${i.guild.channels.cache.size}**\nRoles: **${i.guild.roles.cache.size}**\nBoosts: **${i.guild.premiumSubscriptionCount||0}**`)]});
if(['userinfo','avatar','banner'].includes(n)){const u=i.options.getUser?.('user')||i.user;if(n==='avatar')return i.reply(u.displayAvatarURL({size:1024}));if(n==='banner'){const x=await client.users.fetch(u.id,{force:true});return i.reply(x.bannerURL({size:1024})||'This user has no banner.');}return i.reply({embeds:[E(`👤 ${u.tag}`,`ID: \`${u.id}\`\nCreated: <t:${Math.floor(u.createdTimestamp/1000)}:R>`).setThumbnail(u.displayAvatarURL({size:256}))]});}
if(n==='poll'){const q=i.options.getString('question')||'Poll',o=(i.options.getString('options')||'Yes|No').split('|').filter(Boolean).slice(0,10),l=['🇦','🇧','🇨','🇩','🇪','🇫','🇬','🇭','🇮','🇯'];const m=await i.channel.send({embeds:[E(`📊 ${q}`,o.map((x,k)=>`${l[k]} ${x}`).join('\n\n'))]});for(let k=0;k<o.length;k++)await m.react(l[k]).catch(()=>{});return i.reply({content:`✅ Poll created: ${m}`,ephemeral:true});}
if(n==='say'){await i.channel.send(i.options.getString('message'));return i.reply({content:'✅ Sent.',ephemeral:true});}
if(n==='announce'){const ch=i.options.getChannel('channel'),msg=i.options.getString('message'),ping=i.options.getString('ping')||'none',role=i.options.getRole('role'),title=i.options.getString('title')||'📢 Announcement';const content=ping==='everyone'?'@everyone':ping==='here'?'@here':ping==='role'&&role?role.toString():'';await ch.send({content,embeds:[E(title,msg)],allowedMentions:{parse:['everyone','here'].includes(ping)?[ping]:[],roles:ping==='role'&&role?[role.id]:[]}});return i.reply({content:`✅ Announcement sent to ${ch}.`,ephemeral:true});}
if(n==='embed'){const ch=i.options.getChannel('channel')||i.channel,c=i.options.getString('color')||'5865F2',v=/^#?[0-9a-f]{6}$/i.test(c)?parseInt(c.replace('#',''),16):0x5865f2;await ch.send({embeds:[new EmbedBuilder().setTitle(i.options.getString('title')).setDescription(i.options.getString('description')).setColor(v)]});return i.reply({content:`✅ Embed sent to ${ch}.`,ephemeral:true});}
if(n==='calculator'){const x=i.options.getString('expression');if(!/^[0-9+\-*/().% ]+$/.test(x))return i.reply('Only basic arithmetic is supported.');try{return i.reply(String(Function(`"use strict";return (${x})`)()));}catch{return i.reply('Invalid expression.');}}
if(['kick','ban','timeout','untimeout','warn'].includes(n)){const m=await i.guild.members.fetch(i.options.getUser('user').id).catch(()=>null);if(!m)return i.reply({content:'Member not found.',ephemeral:true});const r=i.options.getString?.('reason')||'No reason provided';try{if(n==='kick')await m.kick(r);if(n==='ban')await m.ban({reason:r});if(n==='timeout')await m.timeout(i.options.getInteger('duration')*1000,r);if(n==='untimeout')await m.timeout(null,r);if(n==='warn')db.prepare('INSERT INTO warns VALUES(?,?,?,?,?)').run(i.guildId,m.id,r,i.user.id,Date.now());return i.reply({embeds:[E('🛡️ Moderation',`Action: **${n}**\nMember: ${m.user}\nReason: ${r}`)]});}catch(err){return i.reply({content:`❌ ${err.message}`,ephemeral:true});}}
if(n==='unban'){await i.guild.members.unban(i.options.getString('user_id'),i.options.getString('reason')||'No reason');return i.reply('🔓 User unbanned.');}
if(['purge','clear'].includes(n)){const a=i.options.getInteger('amount');await i.channel.bulkDelete(a,true);return i.reply({content:`🧹 Deleted ${a} messages.`,ephemeral:true});}if(['lock','unlock'].includes(n)){await i.channel.permissionOverwrites.edit(i.guild.roles.everyone,{SendMessages:n==='unlock'});return i.reply(`🔒 Channel ${n==='lock'?'locked':'unlocked'}.`);}if(n==='slowmode'){await i.channel.setRateLimitPerUser(i.options.getInteger('seconds'));return i.reply('🐢 Slowmode updated.');}
if(n==='nick'){const m=await i.guild.members.fetch(i.options.getUser('user').id);await m.setNickname(i.options.getString('nickname'));return i.reply('✅ Nickname updated.');}if(['addrole','removerole'].includes(n)){const m=await i.guild.members.fetch(i.options.getUser('user').id),r=i.options.getRole('role');n==='addrole'?await m.roles.add(r):await m.roles.remove(r);return i.reply('✅ Role updated.');}if(n==='role'){const r=i.options.getRole('role');return i.reply({embeds:[E(`🎭 ${r.name}`,`ID: ${r.id}\nMembers: ${r.members.size}\nPosition: ${r.position}`)]});}
if(['lockdown','unlockdown'].includes(n)){let c=0;for(const ch of i.guild.channels.cache.values())if(ch.isTextBased()&&ch.permissionOverwrites){await ch.permissionOverwrites.edit(i.guild.roles.everyone,{SendMessages:n==='unlockdown'}).catch(()=>{});c++;}return i.reply(`🔐 Updated ${c} channels.`);}if(n==='nuke'){const old=i.channel,copy=await old.clone();await copy.setPosition(old.position);await old.delete();return copy.send('💥 Channel recreated.');}
if(['warnings','clearwarnings'].includes(n)){const u=i.options.getUser('user')||i.user;if(n==='clearwarnings'){db.prepare('DELETE FROM warns WHERE guild=? AND user=?').run(i.guildId,u.id);return i.reply('🧹 Warnings cleared.');}const rows=db.prepare('SELECT * FROM warns WHERE guild=? AND user=? ORDER BY at DESC LIMIT 20').all(i.guildId,u.id);return i.reply({embeds:[E(`⚠️ Warnings • ${u.tag}`,rows.length?rows.map((r,k)=>`${k+1}. ${r.reason} • <@${r.by}>`).join('\n'):'No warnings.')]});}
if(['automod','antilink','antispam','antiraid','antialt','verification','welcome','goodbye','autorole','setmodlog','setlog','config','starboard','suggest','afk'].includes(n))return features.feature(i,n,{db,client,prefix});
if(['level','rank','xp'].includes(n)){const u=i.options.getUser('user')||i.user,r=db.prepare('SELECT * FROM xp WHERE guild=? AND user=?').get(i.guildId,u.id)||{xp:0,level:0};return i.reply({embeds:[E(`📈 ${n==='rank'?'Rank':n==='level'?'Level':'XP'}`,`${u}\nLevel: **${r.level}**\nXP: **${r.xp}**\nNext: **${leveling.xpFor(r.level)} XP**`)]});}
if(n==='leaderboard'){const rows=db.prepare('SELECT user,level,xp FROM xp WHERE guild=? ORDER BY level DESC,xp DESC LIMIT 10').all(i.guildId);return i.reply({embeds:[E('🏆 Level Leaderboard',rows.length?rows.map((r,k)=>`**${k+1}.** <@${r.user}> — Level ${r.level} (${r.xp} XP)`).join('\n'):'No XP data yet.')]});}
if(['setxp','addxp','removexp'].includes(n)){const u=i.options.getUser('user'),a=i.options.getInteger('amount');let r=db.prepare('SELECT * FROM xp WHERE guild=? AND user=?').get(i.guildId,u.id);if(!r){db.prepare('INSERT INTO xp VALUES(?,?,?,?)').run(i.guildId,u.id,0,0);r={xp:0,level:0};}let total=r.level*100+r.xp;if(n==='setxp')total=a;if(n==='addxp')total+=a;if(n==='removexp')total=Math.max(0,total-a);const level=Math.floor(total/100),xp=total%100;db.prepare('UPDATE xp SET xp=?,level=? WHERE guild=? AND user=?').run(xp,level,i.guildId,u.id);return i.reply(`📈 ${u} is now level **${level}** with **${xp} XP**.`);}
if(n==='ticketpanel'){const ch=i.options.getChannel('channel');await ch.send(tickets.panel(db,i.guildId));return i.reply({content:`🎫 Ticket panel sent to ${ch}.`,ephemeral:true});}if(n==='ticketcategory'){const id=i.options.getString('id'),title=i.options.getString('name'),emoji=i.options.getString('emoji')||'🎫',cat=i.options.getChannel('category'),staff=i.options.getRole('staff_role');tickets.ensureGeneral(db,i.guildId);db.prepare('INSERT INTO ticket_categories(guild,id,name,emoji,category,staff_role) VALUES(?,?,?,?,?,?) ON CONFLICT(guild,id) DO UPDATE SET name=excluded.name,emoji=excluded.emoji,category=excluded.category,staff_role=excluded.staff_role').run(i.guildId,id,title,emoji,cat?.id||null,staff?.id||null);return i.reply(`🎫 Ticket category **${title}** saved.`);}if(n==='ticketcategories'){const rows=db.prepare('SELECT * FROM ticket_categories WHERE guild=? ORDER BY name').all(i.guildId);return i.reply({embeds:[E('🎫 Ticket Categories',rows.length?rows.map(r=>`${r.emoji} **${r.name}** — \`${r.id}\``).join('\n'):'No categories configured.')]});}if(['ticketlogs','tickettranscript'].includes(n)){const ch=i.options.getChannel('channel');tickets.ensureGeneral(db,i.guildId);const col=n==='ticketlogs'?'log_channel':'transcript_channel';db.prepare(`UPDATE ticket_categories SET ${col}=? WHERE guild=?`).run(ch.id,i.guildId);return i.reply(`✅ ${n==='ticketlogs'?'Ticket logs':'Ticket transcripts'} set to ${ch}.`);}if(n==='ticketsettings'){const rows=db.prepare('SELECT * FROM ticket_categories WHERE guild=?').all(i.guildId);return i.reply({embeds:[E('⚙️ Ticket Settings',rows.map(r=>`${r.emoji} **${r.name}**\nStaff: ${r.staff_role?`<@&${r.staff_role}>`:'none'} • Logs: ${r.log_channel?`<#${r.log_channel}>`:'none'} • Transcripts: ${r.transcript_channel?`<#${r.transcript_channel}>`:'none'}`).join('\n\n')||'No categories.')]});}if(n==='ticket'){const id=i.options.getString('category'),cat=id?db.prepare('SELECT * FROM ticket_categories WHERE guild=? AND id=?').get(i.guildId,id):tickets.ensureGeneral(db,i.guildId);return tickets.create(i,db,cat);}
if(n==='applicationpanel'){const ch=i.options.getChannel('channel');await ch.send(applications.panel(db,i.guildId));return i.reply({content:`📝 Application panel sent to ${ch}.`,ephemeral:true});}if(n==='applicationcreate'){const id=i.options.getString('id'),title=i.options.getString('title'),description=i.options.getString('description'),review=i.options.getChannel('review_channel');db.prepare('INSERT INTO application_forms(guild,id,title,description,review_channel) VALUES(?,?,?,?,?) ON CONFLICT(guild,id) DO UPDATE SET title=excluded.title,description=excluded.description,review_channel=excluded.review_channel').run(i.guildId,id,title,description,review?.id||null);return i.reply(`📝 Application form **${title}** saved.`);}if(n==='applicationquestions'){const qs=i.options.getString('questions').split('|').map(x=>x.trim()).filter(Boolean).slice(0,5);db.prepare('UPDATE application_forms SET questions=? WHERE guild=? AND id=?').run(JSON.stringify(qs),i.guildId,i.options.getString('id'));return i.reply(`✅ Saved ${qs.length} questions.`);}if(['applications','applicationsettings'].includes(n)){const rows=n==='applications'?db.prepare('SELECT * FROM applications WHERE guild=? ORDER BY id DESC LIMIT 15').all(i.guildId):db.prepare('SELECT * FROM application_forms WHERE guild=? ORDER BY title').all(i.guildId);return i.reply({embeds:[E(n==='applications'?'📝 Recent Applications':'⚙️ Application Forms',rows.length?rows.map(r=>n==='applications'?`#${r.id} • <@${r.user}> • **${r.status}** • ${r.form_id}`:`**${r.title}** — \`${r.id}\``).join('\n'):'None configured.')]});}if(n==='applicationreview'){const id=i.options.getInteger('id'),action=i.options.getString('action');db.prepare('UPDATE applications SET status=?,reviewer=? WHERE guild=? AND id=?').run(action,i.user.id,i.guildId,id);return i.reply(`📝 Application #${id} marked **${action}**.`);}if(n==='application')return i.reply(applications.panel(db,i.guildId));
if(['balance','daily','work','pay','shop','buy'].includes(n)){const w=wallet(i.user.id);if(n==='balance')return i.reply(`💰 Balance: **${w.balance}** coins`);if(n==='daily'){if(Date.now()-w.daily<86400000)return i.reply({content:'Daily already claimed.',ephemeral:true});db.prepare('UPDATE money SET balance=balance+500,daily=? WHERE user=?').run(Date.now(),i.user.id);return i.reply('🎁 You received **500** coins.');}if(n==='work'){const a=100+Math.floor(Math.random()*401);db.prepare('UPDATE money SET balance=balance+? WHERE user=?').run(a,i.user.id);return i.reply(`💼 You earned **${a}** coins.`);}if(n==='pay'){const u=i.options.getUser('user'),a=i.options.getInteger('amount');if(a>w.balance)return i.reply('Not enough coins.');wallet(u.id);db.prepare('UPDATE money SET balance=balance-? WHERE user=?').run(a,i.user.id);db.prepare('UPDATE money SET balance=balance+? WHERE user=?').run(a,u.id);return i.reply(`💸 Paid ${u} **${a}** coins.`);}if(n==='shop')return i.reply('🛒 Shop: `starter` (100), `vip` (500), `booster` (1000).');return i.reply('🛒 Configure per-server shop items to enable purchases.');}
if(['play','pause','resume','skip','stop','queue','nowplaying','volume'].includes(n))return i.reply({embeds:[E('🎵 Music',`**/${n}** is registered. Audio playback requires the music provider/player to be configured.`)]});
return i.reply({embeds:[E(`/${n}`,'This command is registered and permission-controlled.')],ephemeral:true});}

async function syncCommands(){const rest=new REST({version:'10'}).setToken(token),body=slashCommands.map(c=>c.toJSON()),app=client.user.id;if(body.length>100)throw new Error(`Discord permits at most 100 global chat-input commands; found ${body.length}`);console.log(`🧹 Replacing application commands with ${body.length} slash commands...`);await rest.put(Routes.applicationCommands(app),{body});const test=process.env.DEV_GUILD_ID||process.env.REGISTER_GUILD_ID;if(test){await rest.put(Routes.applicationGuildCommands(app,test),{body});console.log(`⚡ Synced ${body.length} commands to test guild ${test}.`);}console.log(`✅ Slash command sync complete: ${body.length} commands.`);}
client.once('ready',async()=>{console.log(`🔑 Logged in as ${client.user.tag}`);try{for(const g of client.guilds.cache.values()){features.ensureSetting(db,g.id,prefix);await invites.cacheGuild(g,db).catch(()=>{});await statistics.updateCounters(g,db).catch(()=>{});}logging.bind(client,db);features.startSchedulers(client,db);await syncCommands();console.log(`🚀 Ready: ${client.guilds.cache.size} servers | ${slashCommands.length} slash | ${prefixCommands.length} client`);}catch(e){console.error('❌ Startup/sync error:',e);}});
client.on('interactionCreate',async i=>{try{if(i.isChatInputCommand())return await run(i,i.commandName);const tr=await tickets.handle(i,{db,client,prefix});if(tr!==false)return;const ar=await applications.handle(i,{db,client,prefix});if(ar!==false)return;if(i.isStringSelectMenu()&&i.customId==='help:category')return i.update({embeds:[commandListEmbed(i.values[0])],components:i.message.components});if(i.isButton()&&i.customId==='help:slash')return i.reply({content:`There are **${slashCommands.length}** slash commands.`,ephemeral:true});if(i.isButton()&&i.customId==='help:prefix')return i.reply({content:`There are **${prefixCommands.length}** client commands. Prefix: \`${prefix}\``,ephemeral:true});if(i.isStringSelectMenu()&&i.customId==='servers:select'){const g=client.guilds.cache.get(i.values[0]);if(!g)return i.reply({content:'Server not found.',ephemeral:true});return i.update({embeds:[E(`🌐 ${g.name}`,`Members: **${g.memberCount||'?'}**\nThe bot is installed in this server.`)],components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setLabel('Invite Bot').setStyle(ButtonStyle.Link).setURL(inviteUrl(g.id)))]});}}catch(e){console.error('interaction error:',e);if(!i.replied&&!i.deferred)await i.reply({content:`❌ ${e.message}`,ephemeral:true}).catch(()=>{});}});
client.on('messageCreate',async m=>{if(!m.guild||m.author.bot)return;await statistics.recordMessage(db,m.guild.id,m.author.id,m.channel.id).catch(()=>{});await leveling.onMessage(m,db).catch(()=>{});await features.onMessage(m,{db,client,prefix}).catch(()=>{});if(!m.content.startsWith(prefix))return;const parts=m.content.slice(prefix.length).trim().split(/\s+/),raw=(parts.shift()||'').toLowerCase();if(!raw||!prefixCommands.includes(raw))return;const canonical=aliasMap.get(raw)||raw;if(canonical==='help')return m.reply({embeds:[commandListEmbed()]});const fake={guild:m.guild,guildId:m.guild.id,channel:m.channel,channelId:m.channel.id,user:m.author,member:m.member,options:{getUser:()=>m.mentions.users.first()||m.author,getRole:()=>m.mentions.roles.first(),getChannel:()=>m.mentions.channels.first()||m.channel,getString:()=>parts.join(' '),getInteger:()=>Number(parts[0])||0,getBoolean:()=>['true','on','enable','enabled'].includes(String(parts[0]).toLowerCase())},reply:x=>m.reply(typeof x==='string'?x:(x?.content||x))};try{await run(fake,canonical);}catch(e){await m.reply(`❌ ${e.message}`).catch(()=>{});}});
client.on('guildMemberAdd',async m=>{await statistics.member(db,m.guild.id,m.id).catch(()=>{});await statistics.updateCounters(m.guild,db).catch(()=>{});const s=features.getSetting(db,m.guild.id);if(s?.autorole){const r=m.guild.roles.cache.get(s.autorole);if(r)await m.roles.add(r).catch(()=>{});}if(s?.welcome_channel){const c=m.guild.channels.cache.get(s.welcome_channel);if(c)await c.send((s.welcome_message||'Welcome -member-!').replaceAll('-member-',m.toString())).catch(()=>{});}const hit=await invites.onJoin(m,db).catch(()=>null);if(hit&&m.guild.systemChannel)m.guild.systemChannel.send(`📨 ${m.user} joined using an invite from <@${hit.inviter}>.`).catch(()=>{});});
client.on('guildMemberRemove',m=>{const s=features.getSetting(db,m.guild.id);if(s?.goodbye_channel){const c=m.guild.channels.cache.get(s.goodbye_channel);if(c)c.send((s.goodbye_message||'Goodbye -member-!').replaceAll('-member-',m.user.tag)).catch(()=>{});}invites.onLeave(m,db).catch(()=>{});statistics.updateCounters(m.guild,db).catch(()=>{});});
setInterval(()=>{for(const g of client.guilds.cache.values())statistics.updateCounters(g,db).catch(()=>{});},300000);
const port=Number(process.env.PORT)||10000;http.createServer((req,res)=>{res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify({status:'ok',bot:client.user?.tag||'starting',uptime:Math.floor((Date.now()-startedAt)/1000),slash:slashCommands.length,client:prefixCommands.length}));}).listen(port,'0.0.0.0',()=>console.log(`🌐 Web server listening on 0.0.0.0:${port}`));
client.login(token);
